from rna import train

'''
START_WEIGHTS = [0, 0, 0]
LEARNING_RATE = 0.5

observations = [
    [0, 0],
    [0, 1],
    [1, 0],
    [1, 1],
]

labels = [0, 1, 1, 1]

final_weights = train(10, LEARNING_RATE, START_WEIGHTS, observations, labels)

print(final_weights)
'''